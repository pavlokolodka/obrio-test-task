export enum RabbitMQExchange {
  USER_SIGNUP_NOTIFICATION = 'user-signup-notification',
}

export enum RabbitMQRoutingKey {
  SEND_SIGNUP_NOTIFICATION = 'send-signup-notification',
}

export enum RabbitMQQueue {
  SEND_SIGNUP_NOTIFICATION = 'send-signup-notification-queue',
}
